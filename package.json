{
  "name": "@pms/sites-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Per-tenant hotel website platform: themes, booking widget, provisioner",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/**' build",
    "typecheck": "pnpm -r --filter './packages/**' typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
